@import '../../global'

.auth-pre-button
  margin: 8px 0;

.auth-pre-button__button
  button-schema();
  button-variant($btn-primary-color, $btn-primary-bg, $btn-primary-border)
  width: 100%

.auth-pre-button__icon
  vmg-glyph('soc')
  color: #ccc

.auth-pre-button__lbl
  margin-right: 8px
  color: #ccc
