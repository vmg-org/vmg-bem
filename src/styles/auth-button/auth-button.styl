@import '../../global'

.auth-button
  margin: 8px 0;

.auth-button__button
  button-schema();
  button-variant($btn-primary-color, $btn-primary-bg, $btn-primary-border)
  width: 100%

.auth-button__icon
  vmg-glyph('soc')
  color: white

.auth-button__lbl
  margin-right: 8px
